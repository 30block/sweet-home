version: '2.1'
services:
  home:
    build: .
    privileged: true
    network_mode: host
    environment:
      # The supervisor is overriding this value 
      # ensure the right one is used
      USER: 'me'
      TZ: 'America/Santiago'
    volumes:
      - home:/home/me

volumes:
  home:
